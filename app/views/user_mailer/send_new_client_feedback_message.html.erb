<!DOCTYPE html>
<html>
<head>
  <title>Propimo | Отзыв от клиента <%= @client_feedback.id %></title>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <style>
      body {
          font-family: Arial, Verdana, sans-serif;
          font-size: 14px;
          line-height: 1.5;
          color: #555;
          background-color: #f7f7f7;
      }
      .container {
          max-width: 600px;
          margin: 0 auto;
          padding: 20px;
          background-color: #fff;
          border-radius: 10px;
          box-shadow: 0 0 10px rgba(0,0,0,0.1);
      }
      h1 {
          font-size: 24px;
          margin-top: 0;
          margin-bottom: 10px;
          color: #2e6da4;
      }
      p {
          margin-top: 0;
          margin-bottom: 10px;
      }
      a {
          color: #2e6da4;
          text-decoration: none;
      }
      a:hover {
          text-decoration: underline;
      }
  </style>
</head>
<body>
<div class="container">
  <h1>Propimo | Отзыв от клиента <%= @client_feedback.id %></h1>
  <p>Здравствуйте,</p>
  <p>У вас новый отзыв от клиента:</p>
  <ul>
    <li><strong><%= t(:client, scope: :devise) %>:</strong> <%= @client_feedback.name %></li>
    <li><strong><%= t(:phone, scope: :devise) %>:</strong> <%= @client_feedback.phone %></li>
    <li><strong><%= t(:comment, scope: :devise) %>:</strong> <%= @client_feedback.message %></li>
    <% if @client_feedback.contact_ways.present? %>
    <li><strong><%= t(:want_to_have_answer, scope: :contact_ways) %>:</strong>
      <ul>
        <% @client_feedback.contact_ways.each do |contact_way| %>
          <li><%= contact_way.way_type %></li>
        <% end %>
      </ul>
    </li>
    <% end %>
  </ul>
  <p>Для просмотра перейдите на страницу <a href="<%= client_feedbacks_url %>">propimo.com</a></p>
</div>
</body>
</html>

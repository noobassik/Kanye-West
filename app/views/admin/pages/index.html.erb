<% title @page_title %>
<h2><%= t(:index, scope: :pages) %>
  <%#= link_to t("common.add"), new_page_path, class: 'btn btn-primary' %>
</h2>

<table class="table table-striped table-hover table-with-items">
  <tr>
    <th>№</th>
    <th><%= "Title Ru" %></th>
    <th><%= "Title En" %></th>
    <th><%= "H1 Ru" %></th>
    <th><%= "H1 En" %></th>
    <th></th>
  </tr>

  <% @objects.each_with_index do |page, index| %>
    <tr>
      <td><%= index + 1 %></td>
      <td><%= page.title_ru %></td>
      <td><%= page.title_en %></td>
      <td><%= page.h1_ru %></td>
      <td><%= page.h1_en %></td>
      <td>
        <%= render_compact_control_buttons(obj: page,
                                           show_obj_path: page.seo_path,
                                           edit_obj_path: edit_page_path(page)) %>
      </td>
    </tr>
  <% end %>
</table>

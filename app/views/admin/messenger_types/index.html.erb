<% title @page_title %>
<h2><%= t(:index, scope: :messenger_types) %>
  <%= link_to t("common.add"), new_messenger_type_path, class: 'btn btn-primary' %>
</h2>

<table class="table table-striped table-hover table-with-items">
  <tr>
    <th>№</th>
    <th></th>
    <th><%= "Title" %></th>
    <th></th>
  </tr>

  <% @objects.each_with_index do |messenger_type, index| %>
      <tr>
        <td><%= index + 1 %></td>
        <td><i class="<%= messenger_type.icon_class %>"></i></td>
        <td><%= messenger_type.title %></td>
        <td>
          <%= render_compact_control_buttons(obj: messenger_type,
                                             edit_obj_path: edit_messenger_type_path(messenger_type),
                                             confirm_message: t(:delete_confirm, scope: :messenger_types)) %>
        </td>
      </tr>
  <% end %>
</table>

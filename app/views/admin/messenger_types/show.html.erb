<% title @page_title %>
<p id="notice"><%= notice %></p>

<p>
  <i class="<%= @object.icon_class %>"></i>
</p>

<p>
  <strong><%= "Title" %>:</strong>
  <%= @object.title %>
</p>

<%= link_to t(:edit, scope: :common), edit_messenger_type_path(@object), class: 'btn btn-default', title: t(:edit, scope: :common) %>
